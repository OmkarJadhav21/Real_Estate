import { BlurdirDirective } from './blurdir.directive';

describe('BlurdirDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurdirDirective();
    expect(directive).toBeTruthy();
  });
});
